---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: Wiremock
description: WireMock DevService for testing
#description: Do something useful.
metadata:
  keywords:
    - wiremock
    - test
  categories:
    - "testing"
  status: "preview"